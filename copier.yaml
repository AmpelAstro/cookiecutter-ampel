project_name:
  placeholder: SomeTelescope
  type: str
github_org_or_username:
  default: AmpelAstro
  type: str
description:
  default: '{{project_name}} support for the Ampel system'
  type: str
author_full_name:
  default: Alexandra Stronomer
  type: str
author_email:
  default: a.stronomer@mail.to
  type: str
version:
  default: 0.10.0a0
  type: str
